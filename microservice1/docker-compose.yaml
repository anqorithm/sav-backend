version: "3.8"
name: sav-backend
services:
  microservice1:
    container_name: microservice1
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: always
    env_file:
      - ./.env
    ports:
      - "9008:9000"
    volumes:
      - ./:/app
      - ./node_modules:/app/node_modules
    networks:
      - sav-network
networks:
  sav-network:
    name: sav-network
